import * as React from "react";
import * as ReactDOM from "react-dom";

function render() {
  ReactDOM.render(
    <>
      <h2>Hello from React!</h2>
      <span>This is a span, just wanna test the prettier installation.</span>
    </>,
    document.body
  );
}

render();
